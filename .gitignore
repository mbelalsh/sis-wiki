# macOS
.DS_Store

# Source materials — kept local-only per project policy (see CLAUDE.md → Repository Hygiene)
raw/
*.pdf

# Obsidian workspace state (personal — not shared)
wiki/.obsidian/

# Claude Code machine-local settings (personal — not shared)
.claude/settings.local.json
